# Core dependencies for the Vertex AI pipeline.
# By listing only the top-level packages, we allow pip to resolve the
# entire dependency tree with the latest compatible versions.
google-cloud-aiplatform
kfp
